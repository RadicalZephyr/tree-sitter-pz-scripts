[package]
name = "tree-sitter-pz-scripts"
description = "pz-scripts grammar for the tree-sitter parsing library"
version = "0.0.1"
keywords = ["incremental", "parsing", "pz-scripts"]
categories = ["parsing", "text-editors"]
repository = "https://github.com/RadicalZephyr/tree-sitter-pz-scripts"
edition = "2021"
license = "ISC"

build = "bindings/rust/build.rs"
include = [
  "bindings/rust/*",
  "grammar.js",
  "queries/*",
  "src/*",
  "tree-sitter.json",
]

[lib]
path = "bindings/rust/lib.rs"

[dependencies]
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1.2"

[dev-dependencies]
tree-sitter = "0.27"
