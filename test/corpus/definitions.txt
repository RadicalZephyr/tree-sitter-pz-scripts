===
Item
===

module Item {
  item Pepper {
    DisplayName = Camping,
  }
}

---

(module (identifier)
  (item (identifier)
    (item_attribute
      (identifier)
      (item_attribute_value))))

===
Item Multiple Attributes
===

module Item {
  item Pepper {
    Type        = Normal,
    DisplayName = Camping,
  }
}

---

(module (identifier)
  (item (identifier)
    (item_attribute
      (identifier)
      (item_attribute_value))
    (item_attribute
      (identifier)
      (item_attribute_value))))

============
Basic Recipe
============

module Umbrella {
  recipe Close Umbrella
  {
    UmbrellaWhite,

    Result:ClosedUmbrellaWhite,
    Time:10.0,
  }
}

---

(module (identifier)
  (recipe (name)
    (recipe_requirements (recipe_direct_req (identifier)))
    (recipe_attributes
      (recipe_attribute (identifier) (identifier))
      (recipe_attribute (identifier) (number)))))
